import { StyleSheet } from 'react-native';

export const MainStyle = StyleSheet.create({
    body: {
        width: '100%',
        alignItems: 'center',
        justifyContent: "center",
    },
  
    titleText: {
        fontSize: 25,
    },
  
    text: {
        fontSize: 15,
    },
});